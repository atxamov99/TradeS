# Done

## Web (`web/`)
- **Найден и исправлен корень бага регистрации**: `web/vite.config.js` проксировал `/api` на порт `5050` (закомментировано как "macOS фикс"), а бэкенд реально слушает `5000` — поэтому все запросы с сайта на API молча падали. Исправлено на `5000`.
- **Тема**: убран неоновый зелёно-бирюзовый "cyber" дизайн (`#0E150F`/`#2ECC71`/`#1ABC9C` + блюр-декорации) на Landing/Login/Register/AIAssistant. Везде — простой брендовый зелёный (`#2ECC71`) в чистом light/dark режиме через Tailwind `primary-*` токены.
- Добавлен рабочий переключатель light/dark (в коде уже был `themeStore`, просто не был подключён к этим страницам) — сохраняется между перезагрузками (`applyTheme()` вызывается в `main.jsx`).
- **Языковой баг**: на Landing.jsx язык не нормализовался (`i18n.language` мог быть `ru-RU`, а переводы лежат под ключом `ru`) — из-за этого страница держалась на узбекском, хотя бейдж показывал "ru-RU". Приведено в соответствие с Login/Register.
- Извлечены логотип light/dark из присланного фото в `web/public/brand/logo-light.png` (чистый, прозрachный фон) и `logo-dark.png` (фон пока не прозрачный — не встроены в UI).

## Mobile (`mobile/`)
- Тем было 6 (Light, POS Dark, Earthy/Warm, Cyber Organic, Neon Edit, Deep Sea Loft) — оставлено 2: Light и Dark, обе с зелёным акцентом (`#2ECC71`).
- `constants/theme.js`: COLORS.primary переведён с синего на зелёный (для консистентности с брендом).

## Backend (`backend/`)
- Подтверждено: cross-platform логин работает на уровне кода — мобильный и веб используют одинаковую нормализацию телефона (`+`+цифры), совпадающую с тем, что сохраняет `verifyOtp` в БД.
- Живой тест: создан тестовый аккаунт через уже существующий `/auth/register` (телефон+пароль, без Telegram) и залогинен через настоящий UI сайта (`/login` → `/dashboard`) — подтверждает, что вход работает и что зелёная тема применилась глобально (видно и в Dashboard, не только на 3 правленых страницах).
- Добавлен long-polling режим для Telegram-бота (`telegramService.startPolling()`), чтобы не зависеть от ngrok/webhook при локальной разработке.
- **Telegram-бот подключён**: токен получен и добавлен в `backend/.env`, бот подтверждён (`@trades_uz_bot` через `getMe`), long-polling стартует автоматически при `npm run dev`. Инструкция по настройке — в `backend/README.md`.

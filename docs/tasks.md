# Tasks (в работе / ожидают)

## Сейчас
- [x] Получить TELEGRAM_BOT_TOKEN от @BotFather для `trades_uz_bot` и добавить в `backend/.env` — готово, бот `@trades_uz_bot`, токен рабочий (подтверждено через `getMe`)
- [ ] Проверить регистрацию end-to-end вживую: написать `/start` боту `@trades_uz_bot`, поделиться контактом, запросить код в приложении, ввести код — долгий поллинг уже подключён и стартует автоматически (`telegramService.startPolling()`), осталось пройти сценарий руками
- [ ] Решить, как сделать бота "постоянным" в проде — деплой backend на Render (render.yaml уже есть) + `setWebhook` на стабильный Render URL вместо long-polling (long-polling — только для локальной разработки)

## Безопасность — многоустройственные сессии (описано пользователем, не реализовано)
- [ ] Пользователь должен видеть список активных устройств/сессий на своём аккаунте (2-3 устройства и т.д.)
- [ ] Только "исходное" устройство (то, с которого была первая регистрация/первый логин) может выгонять (revoke) другие сессии
- [ ] Вход с НОВОГО (нераспознанного) устройства должен требовать доп. код подтверждения (через тот же Telegram-бот) — обычного телефон+пароль недостаточно
- [ ] Подтверждено дизайном уже сейчас: Telegram-бот шлёт код только в тот чат, что подтверждён шарингом контакта на этот номер (нельзя получить чужой код) — это уже так работает, менять не нужно
- Требует отдельного планирования (схема сессий/устройств в БД, UI списка устройств, revoke-эндпоинт, гейт на логине) — не начинать без отдельного брейнсторминга

## После бота
- [ ] Логотип: вписать light/dark PNG (`web/public/brand/logo-*.png`) в навбар/футер Landing/Login/Register вместо иконки ShoppingBag
  - dark-версия лого требует чистки фона (у фото неровный тёмный фон, простая заливка прозрачности не сработала)
- [x] Landing: смягчить переход hero → features — добавлен scroll-mt-20 на секции-якоря + scroll-reveal анимации (whileInView) на Features/Stats/FAQ + плавные transition на dropdown (язык) и раскрытие FAQ
- [ ] Ручной клик-тест реального сценария: регистрация с телефона (mobile) → логин с тем же номером/паролем на сайте (web)
- [ ] Запустить mobile (Expo) и web одновременно для ручной проверки пользователем

## Возможно позже
- [ ] Убрать неиспользуемый второй i18n (I18nProvider/useI18n в `web/src/i18n/index.jsx`) — **уточнение 2026-07-18:** не совсем "мёртвый": `main.jsx` монтирует `I18nProvider` на весь дерево, и 10 страниц реально зовут `useI18n()` (admins/audit-logs/content/permissions/profile/reports/roles/users — похоже на старый admin-шаблон). `react-i18next` (`src/i18n.js`) — параллельно, отдельно, и это то, что реально использует Login/Register/Landing. Т.е. два независимых, оба живых i18n-стека одновременно — реальный техдолг, но "убрать" = смигрировать 10 страниц на `react-i18next`, не просто удалить файл. Отдельная задача, не one-liner.
- [ ] `SMS_DEMO=true` в backend/.env — мёртвая настройка, нигде не используется в коде (старый SMS-OTP флоу, заменён на Telegram-OTP)
- [x] `forgot_password` — **проверено 2026-07-18, уже не баг:** ключ есть в `web/src/i18n.js` (реальный react-i18next конфиг, который использует Login.jsx) для uz/ru/en, плюс на Login.jsx уже стоит fallback (`t('forgot_password') || '...'`). Похоже было исправлено раньше без обновления этого пункта.
- [ ] Мусорные файлы в корне репо (`nul`, `expo`, `savdo-mobile@1.0.0`, `package-lock.json` в корне, папка `«`) — похоже на обломки случайных команд, ждём подтверждения на удаление. **Уточнение 2026-07-18:** подтверждено что это мусор (0-байтные файлы/пустая папка `«`), но headless-агент физически не может их удалить — `rm`/`git rm` жёстко заблокированы в этой среде (та же категория что `git push`). Нужен человек с реальным доступом к файловой системе (пара секунд: `rm nul expo "savdo-mobile@1.0.0" && rm -rf «` из корня репо). То же касается дубликата `backend/src/middleware/` (пустая папка с одним `.gitkeep`, реальный код в `middlewares/`, ничего на неё не ссылается — подтверждено grep'ом) — безопасно удалить, но той же причине не через агента.
